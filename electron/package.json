{
  "name": "vibecad-electron",
  "version": "0.1.0",
  "description": "VibeCAD desktop application",
  "author": "VibeCAD",
  "license": "MIT",
  "private": true,
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "prestart": "node ../scripts/ensure-electron-native-deps.mjs",
    "start": "electron .",
    "package": "node ../scripts/ensure-electron-native-deps.mjs && npm run build && electron-builder --dir --config electron-builder.cjs",
    "make": "node ../scripts/ensure-electron-native-deps.mjs && npm run build && electron-builder --config electron-builder.cjs --publish never",
    "make:mac": "node ../scripts/ensure-electron-native-deps.mjs && npm run build && electron-builder --mac --config electron-builder.cjs --publish never",
    "make:win": "node ../scripts/ensure-electron-native-deps.mjs && npm run build && electron-builder --win --config electron-builder.cjs --publish never",
    "make:linux": "node ../scripts/ensure-electron-native-deps.mjs && npm run build && electron-builder --linux --config electron-builder.cjs --publish never",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "drizzle-orm": "^0.45.1",
    "electron-log": "^5.4.4",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.19.24",
    "electron": "^41.0.0",
    "electron-builder": "^26.8.1",
    "tsup": "^8.4.0",
    "typescript": "~5.8.3"
  }
}
